#!/usr/bin/env sh
# Hook de Git: se ejecuta automaticamente antes de cada git push
# Si las pruebas unitarias fallan, el push es CANCELADO.
echo "Ejecutando pruebas unitarias antes de publicar..."
cd "$(git rev-parse --show-toplevel)" && npm run test:ci
